# C-5 Command Flow Controls

Fixture-first command-button and group-flow controller. It validates exact directive identity and package authority, creates dry-run plans against existing A-group/SF contracts, suppresses duplicate directive clicks, and enforces the Cycle 2 state machine. It creates no transport and performs no actual dispatch.
